### Using the python standard libraries ###
    ###(math for cos, fac, log, pi) ###
    ### DEV OPS IS AUTOMATING ALL THE DEVELOPER JOBS BY SCRIPTING SOME OR ALL OF THE STUFF TO DO ###
    ###(os for scripting some stuff) ###


### Define and comment the meaning and usefulness of each of the following imports ###
import math ## adds numeric extensions, angles, exponents, logarithms, etc...
import random ## defaults to giving a random decimal number (0 < x < 1), popular "randrange" can even accept a list as parameter (start, stop, step, _Int class)
import os
import sys
import shutil
import glob
import argparse
import re
import statistics
import urllib.request import urlopen
import smtplib
import datetime
import zlib
import timeit
import doctest
import unittest

